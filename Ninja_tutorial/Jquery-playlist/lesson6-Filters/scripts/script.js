$(document).ready(function(){
    // first and last
    // $("header nav li:first").css({border:"3px solid blue"});
    // $("header nav li:last").css({border:"3px solid blue"});

    // $("contact ul:first").css({border:"3px solid red"});
    // $("contact ul:last").css({border:"3px solid red"});

    // first-child and last-child
    // $("contact ul:first-child").css({border:"3px solid red"});
    // $("contact ul:last-child").css({border:"3px solid red"});

    // odd and even
    // $("header nav li:odd").css({border:"3px solid yellow"});
    // $("header nav li:even").css({border:"3px solid orange"});

    // not
    // $("section:not('#contact')").css({border:"3px solid green"});

    // greater than and less than
    // $("#social-nav li:lt(3)").css({border:"3px solid brown"});
    // $("#social-nav li:gt(2)").css({border:"3px solid grey"});

    // attribute filter
    // $("div[class]").css({border:"3px solid pink"});
    // $("img[alt]").css({border:"3px solid pink"});

    // attribute with specific value
    $("img[alt=quote]").css({border:"3px solid red"});



});